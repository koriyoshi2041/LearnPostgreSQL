PORT=5000
NODE_ENV=development

# Main database (stores user data)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=golden_whisk_main
DB_USER=postgres
DB_PASSWORD=postgres

# Sandbox database template (for user practice)
SANDBOX_DB_PREFIX=golden_whisk_sandbox_
SANDBOX_DB_USER=postgres
SANDBOX_DB_PASSWORD=postgres

# Security
SQL_TIMEOUT_MS=3000
MAX_REQUESTS_PER_MINUTE=10

